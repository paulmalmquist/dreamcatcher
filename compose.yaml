services:
  dreamcatcher:
    build: .
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - dreamcatcher-data:/app/data
    restart: unless-stopped
volumes:
  dreamcatcher-data:
