# Paste into Claude Code or Codex on the work computer

You are integrating the attached Dreamcatcher beta into my work environment. First locate this extracted repo and the existing Paul OS/work app using the current workspace and applicable repository instructions. Do not assume paths or architecture. Treat the work copy as authoritative; this is a one-way transfer from a personal prototype. Do not upload company code, data, internal addresses or secrets back to the personal environment.

Read CLAUDE.md, integrations/CONNECTIONS.json, docs/V2-PLATFORM.md, README.md, docs/ARCHITECTURE.md, docs/RELEASES.md, docs/SECURITY.md and docs/WORK-COMPUTER-HANDOFF.md. Search WORK-CONNECT: for explicit replacement seams. Then inspect the actual work repository's AGENTS.md/CLAUDE.md, package manifests, API/router structure, identity integration, database access conventions and local skill directories. Follow applicable instructions without weakening them. Do not introduce Supabase. Our relevant data stack includes BigQuery, dbt and Power BI, but discover exact infrastructure from local evidence.

Preserve the approved original Dreamcatcher gallery and animated Three.js space design. Its primary actions are Download SDK and Upload your app, mainly URL registration. Appsmith is a reference for capability inventory; do not adopt its UI or migrate this into an Appsmith fork.

Proceed autonomously on reversible local work. Start in an isolated branch or worktree if appropriate and preserve existing user changes. Run the standalone build, API tests, SDK tests and scripts/doctor.py to establish a baseline. Report actual failures; never mark company integrations verified based on mocks. Do not install unapproved dependencies or perform production writes without existing authorization.

Create a concrete integration inventory with exact local file paths and explicit unknowns. Identify the smallest compatible integration: prefer a standalone service linked from Paul OS initially, unless the existing architecture clearly supports a module. Reuse the corporate identity/session and authorization conventions where appropriate, with explicit trust boundaries and tests. Preserve a standalone demo mode in a separate data directory. Never promote seeded demo accounts or evidence into the work pilot.

Implement a first pilot using one existing test app and one approved governed data source. Locate actual dbt model/source metadata and available certification artifacts. Trace source → staging → intermediate → mart → consumer, preserving transformation semantics, grain, keys, joins, units and filters. Reconcile the query result with the existing report/semantic model. Do not label a mart or query certified without the existing owners' evidence. Keep query approval and any organizational certification tiers separate until their relationship is explicitly defined.

Enforce app access separately from data access. Discover the real OIDC issuer/subject and group lifecycle, then bind to actual warehouse identity/IAM/row/column policies. Do not silently replace per-caller access with shared privileged credentials. Add positive and negative tests for shared apps, data entitlements, disabled users, stale sessions and revoked query/skill versions.

Connect approved-package policies to the organization's real build process. Require complete direct/transitive dependency inventories and exact version/integrity/source checks. The beta accepts npm locks; if real apps use Python or other package formats, implement appropriate policy adapters and tests rather than omitting those dependencies. A trusted CI job must generate scan evidence, build and attest the immutable artifact, and deploy the same artifact. Never give app authors the signing key or accept self-asserted scan success as verification.

Keep skills transferable as instruction/reference files plus versioned contracts. Discover our existing SKILL.md conventions before adapting. Bind credentials and permissions at the destination and require local reapproval. The supplied beta executes deterministic query workflows only; arbitrary scripts or agent runtimes require a separately approved isolated execution adapter. Do not run uploaded scripts in the registry process. Add a portable round-trip example with environment-free metadata and reproducible expected behavior.

Finish all locally achievable implementation, documentation and tests. If real access is unavailable, leave the connector disabled, document the exact missing input and provide a tested interface using clearly synthetic fixtures. Do not weaken policy, invent credentials or claim that synthetic tests verify BigQuery/SSO. Ask only for genuinely missing decisions that block the next concrete step after completing useful local work.

Deliver: the working branch; integration inventory; a concise change report; test evidence distinguishing local, mocked and live checks; pilot acceptance results; operational setup/backup/rollback instructions; and a list of remaining organizational decisions. Do not deploy, push to a remote repository or change company IAM unless explicitly authorized for that destination.
